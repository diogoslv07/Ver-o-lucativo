export interface TestimonialType {
  name: string;
  role: string;
  profit: string;
  quote: string;
  avatar: string;
}

export interface BenefitType {
  title: string;
  description: string;
  icon: string;
}